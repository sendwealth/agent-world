interface:
  display_name: "Agent World"
  short_description: "Repo-specific patterns and workflows for agent-world"
  default_prompt: "Use the agent-world repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true